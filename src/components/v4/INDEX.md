# ERA v4 components

Components for the `/v4` route at eracx.com.
Aesthetic: Bloomberg / Turley brutalist — Anton display, Archivo Black blocks,
IBM Plex Sans body, JetBrains Mono labels. Section grounds in white, magenta
(--v4-hot), yellow, cobalt, and ink.
Tokens at `src/styles/v4-tokens.css`, component styles at `src/styles/v4-components.css` —
both scoped under `.v4-root` so they never bleed into other routes.
Do not reuse these components outside `/v4`.

## Render order in V4.tsx

V4Statement → V4Warmth → V4WhatEra → V4Evidence → V4HowItWorks → V4Technology
→ V4Lab → V4FAQ → V4Footer

## Components

- **V4Header.tsx** — Bloomberg-style issue bar at the top of every section. Takes a `phase` string (left side, dot-separated) and a `meta` array (right side). Mono uppercase, 0.14em letter-spaced, color inherits from the section.
- **V4Nav.tsx** — Main navigation. Inline ERA symbol mark (single path from public/images/era_symbol.svg, fill="currentColor", height 32px) on the left, mono-uppercase links in the middle (Warmth, Evidence, How it works, Tech, Lab), solid currentColor "Get started" CTA on the right that opens a `mailto:hello@eracx.com`. Border-bottom of 1px currentColor.
- **V4Statement.tsx** — §01 hero / statement section. Two-column layout: massive Anton headline ("Cold / outbound / is over.") left, ink-bordered sidebar (eyebrow, lede, "Evaluate your GTM →" CTA) right. Below that, a 4-cell meta band (5 / 9 / 24 / 80%) with top and bottom 1px ink rules.
- **V4Warmth.tsx** — §02 magenta-ground warmth section. Issue bar + two-column header ("Warmth compounds." Anton mega + IBM Plex lede with ink-highlighted phrase). Two stacked white-card visualizations: (1) Pentagon card — static FRVRD radar for Account 047 (F=92, R=88, V=90, R=84, D=86, composite 88) with five letter-block summary cells below; (2) Timeline card — multi-input chart, 1400×720 viewBox, 14-week trajectory with 10 event markers across Content/Social/Email/Event/Referral channels, quiet stretch shading at W4, signal-cooling event at W7 in muted gray, terminus at W14 reading "DEAL MOVING". 4-cell magenta stat band at the bottom (10 / 14 / 3 / 88). Below 900px the timeline card scrolls horizontally with a "scroll →" mono hint. All data synthetic, clearly tagged.
- **V4WhatEra.tsx** — §03 white-ground "What ERA is / isn't" section. Issue bar + Anton mega headline ("ERA doesn't replace your content." with magenta em on "content") + IBM Plex lede with yellow-highlighted "already producing signals". Three layers: (1) IS/ISN'T two-column grid — white card with magenta marker numbers vs ink-black card with yellow marker numbers, 4 numbered items each, 1px ink rule between, 2px ink outer border; (2) Content stack directory — "Your content is already running." + intro, then 6 rows in a 4-column grid (NUM / source+layer / magenta signal / action) covering white papers, webinars, CEO LinkedIn, conferences, referrals, newsletter; (3) Pull quote — parchment ground (--v4-parchment) with 4px magenta left border, Anton clamp(40px,5vw,72px) "Your content makes the warmth. / We make the warmth visible." (second line magenta), mono "WHAT ERA DOES IN ONE LINE" attribution. Below 900px the IS/ISN'T grid stacks and the directory rows reflow to 2 columns.
- **V4Evidence.tsx** — §04 yellow-ground proof section. Issue bar + two-column header ("Real loops. Real customers." Anton mega ink with muted-ink em on "customers" + IBM Plex lede). Three argument layers: (1) Logo wall — 4 ERA customer marks (Lorikeet, Assemble, Netrush, Navalent) rendered in flat ink via `filter: brightness(0)` opacity 0.85, top + bottom 1px ink rules; (2) Hero quote — Nate Houghton (Lorikeet, Head of Sales) in IBM Plex 700 weight clamp(28px, 3.5vw, 56px) with smart curly quotes via ::before/::after, attribution row with small Lorikeet logo + mono uppercase name/title/company; (3) Bottom row — Lara Vandenberg (Assemble, Founder) supporting quote in a 2px ink-bordered card on the left (1.4fr), Justin Marshall founder portrait on the right (1fr) sized max 220px with grayscale filter and mono caption. White text forbidden on yellow ground; magenta accent rested for this section.
- **V4HowItWorks.tsx** — §05 cobalt-ground (--v4-cobalt) section showing ERA's three loops as a non-linear 3×3 grid. Issue bar + two-column header ("Three loops. Always running." Anton mega white + IBM Plex lede with ink-highlighted "concurrent loops"). 12-cell CSS grid: 1 header row of 3 loop columns (CONNECTION / TRUST / LOYALTY, each with mono "LOOP 0X" subtitle, Anton 48–72px name, IBM Plex one-liner) + 3 stage rows × 3 columns = 9 stage cells. Each stage cell shows mono "STAGE 0X", Archivo Black stage name, IBM Plex one-sentence claim. Stage data ported verbatim from src/components/v3/Loop.tsx — Detect/Enrich/Score (Connection), Reach/Respond/Nurture (Trust), Close/Expand/Retain (Loyalty). Cells separated by white-alpha 0.2 rules, outer border at white-alpha 0.4, no arrows or progressive disclosure. Closing mono summary "9 STAGES · 3 LOOPS · ZERO START AND END". Below 900px the grid collapses to 1 column with each loop header followed by its 3 stages.
- **V4Lab.tsx** — §07 parchment-ground (--v4-parchment) section showing ERA's four free tools as a 2×2 grid of ink-black tiles with rust accents. Issue bar + two-column header ("Tools we built so you can try us." Anton mega ink with italic-rust em on "try us" + IBM Plex lede with `<strong>` on "show what we do when we do it ourselves"). Each tile (Halo / Signal River / Map / AI Mirror) defaults to ink ground / white type: status pill in top-left (LIVE = rust-filled with ink text, BETA = rust-alpha-0.2 with rust text, COMING SOON = transparent with rust outline + rust text), URL in top-right (white-alpha 0.5), Anton mega tool name in white aligned to bottom of top section, 1px white-alpha 0.15 horizontal rule, IBM Plex 17px description (white-alpha 0.85), rust mono uppercase CTA arrow in the bottom-right (the only `<a href>` per tile). Tiles share borders (2px ink between, 4px ink outer frame). Hover inverts the tile to parchment ground / ink type with 120ms transition — rust accents (CTA, status pills) preserved through the inversion. Closing mono "USE THEM · NO LOGIN · NO SALES PITCH". Tile grid collapses to 1 column below 900px.
- **V4FAQ.tsx** — §08 white-ground always-open FAQ. No accordion, no expand/collapse. Issue bar + two-column header ("Worth asking." Anton mega ink with italic-magenta em on "asking" + IBM Plex lede). 5 FAQ blocks rendered as `<article>` items separated by 2px ink rules: mono "Q1" through "Q5" prefix, Archivo Black `clamp(28px, 3vw, 44px)` question, 1px ink-alpha-0.3 internal rule, IBM Plex 17px answer at 1.55 line-height (max-width 70ch). Content ported verbatim from index.html JSON-LD FAQPage with voice corrections applied: Era → ERA, clients → customers, em dashes removed. Closing mono CTA "More questions? hello@eracx.com" with mailto link. SEO data in index.html and on-page section must stay in sync; if FAQ copy changes here, update the JSON-LD too.
- **V4Footer.tsx** — §09 ink-ground footer / colophon. Issue bar (§09 · COLOPHON / VOL ONE · ISSUE 04 · ERACX.COM), centered Anton mega closing manifesto "Warmth is the work." with magenta normal-style em on "work" (bookends §01's "Cold outbound is over."), full ERA logo lockup (all 4 paths from public/images/era-logo.svg with `fill="currentColor"`, viewBox `0 0 20202 4387`, 120px desktop / 80px mobile, centered, white-on-ink). Three-column body separated by white-alpha 0.2 top rule: SECTIONS (8 anchor links to §01–§08), CONTACT (mailto link to hello@eracx.com with magenta hover + one-line copy), COLOPHON (typefaces + stack + issue/volume metadata in IBM Plex 13px). Bottom legal bar: white-alpha 0.5 mono `ERA · A PRODUCT OF DEPARTMENT OF LOYALTY LLC` left, `© 2026 · ALL RIGHTS RESERVED` right. Department of Loyalty LLC fine print appears here only — the only legal-entity mention in v4 marketing surface.
- **V4Technology.tsx** — §06 ink-ground brutalist box grid of all 24 signals. Issue bar + two-column header ("Twenty-four signals. Zero guesswork." Anton mega white with magenta italic em on "guesswork" + IBM Plex lede with magenta `<strong>` on "Every signal has a tech vendor behind it"). 24 boxes in a 3-col desktop / 2-col tablet (701–1100px) / 1-col mobile (≤700px) grid, no gaps — adjacent boxes share borders. Each box: 4px colored top border keyed to category (INTENT magenta, BEHAVIORAL magenta-tint, NETWORK / TECHNOGRAPHIC / FIRMOGRAPHIC descending white-alpha), mono "NN / 24" prefix, Archivo Black signal name, mono uppercase meaning subtitle, 1px white-alpha 0.15 rule, IBM Plex activation copy, mono tech-vendor line with `→ ` prefix, absolute-positioned mono category label in the bottom-right corner. Hover subtly tints background and thickens top border to 6px (no transforms, no shadow). 2px white-alpha 0.4 outer frame around the full grid. Closing summary "24 SIGNALS · 24 TECH VENDORS · ALL FIRING TODAY". Magenta returns here for the first time since §02 — visual thread from "warmth" to "signals."
